# Netlify hosting config. Firebase is used only for data (Firestore) + auth.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: serve index.html for any path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
